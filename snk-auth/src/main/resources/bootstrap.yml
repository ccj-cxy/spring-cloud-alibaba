##微服务配置
spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        name: snk-auth
        group: DEFAULT_GROUP
        namespace:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace:
  application:
    name: snk-auth

logging:
  level:
    com.alibaba.nacos.client.config.impl: WARN
    com:
      alibaba:
        nacos:
          client:
            naming: warn